-- 자유 랭크. 솔로와 별개의 값이라 별도 컬럼이다.

-- 한 컬럼에 "솔로 또는 자유"를 담으면 어느 쪽인지 모르는 값이 된다. 두 큐는 배치도 따로
-- 하고 티어도 따로 움직이므로, 솔로만 있는 계정과 자유만 있는 계정이 모두 흔하다.
ALTER TABLE game_accounts ADD COLUMN flex_rank_code VARCHAR(40);

-- rank_updated_at은 둘 중 하나라도 채워진 시각이다. 한 번의 조회로 둘을 함께 읽으므로
-- (league-v4가 큐별 항목을 한 응답에 준다) 따로 나눌 이유가 없다.
