# Frontend

Owner: Member 1.

## Start
```bash
npm install
npm run dev
```

## First tasks
1. `design/` 레퍼런스를 페이지별로 구현
2. `contracts/openapi.yaml` 기준 mock API adapter 작성
3. backend 없이도 fake queue/proposal/party flow 동작
4. 이후 실제 API/WebSocket 연결
5. PartyRoom에서 WebRTC audio + DataChannel 구현

UI에 디자인 이미지의 잘못된 business data를 복사하지 말 것. `CLAUDE.md` 우선.
