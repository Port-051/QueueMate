# 이 브랜치는 실제 백엔드에 붙는다. mock 검증은 integration/c2 쪽 브랜치에 남겨 둔다.
#
# 기본값이 mock인 이유는 CLAUDE.md 5절이 backend 완성 전에도 프론트를 개발할 수 있어야
# 한다고 요구하기 때문이다. 그 기본값은 건드리지 않고 이 파일로만 덮는다.
#
# 붙을 대상은 vite.config.ts의 proxy가 정한다. 지금은 localhost:8080이다.
VITE_API_MODE=real
